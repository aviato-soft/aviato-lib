language: php

php:
  - 7.3
  - 7.4
  - 8.0

# blocklist
branches:
  except:
  - demo
  - dev
  - main

# safelist
branches:
  only:
  - test

before_script:
  - composer self-update
  - composer install --no-interaction --ignore-platform-reqs

script:
  - ./vendor/bin/phpunit