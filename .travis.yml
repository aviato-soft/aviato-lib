dist: focal

language: php

php:
# - 7.3
  - 7.4
  - 8.0
  - 8.1

# blocklist
branches:
  except:
  - demo
  - dev
  - master

# safelist
branches:
  only:
  - test

before_script:
  - composer self-update
  - composer install --no-interaction --ignore-platform-reqs

script:
  - ./vendor/bin/phpunit